# Changelog

## 1.0.0

- Initial release.
- Patch `SaveableBoatCustomization.GetData` to include sails on inactive masts (all boats).
- Session cache on `LoadData` to recover empty sail lists from the last known rig per `sceneIndex`.
